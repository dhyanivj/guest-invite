import React from "react";

const NotFoundPage = () => {
  return (
    <div>
      NotFoundPage okay!!
      <button className="btn btn-primary">Click me</button>
    </div>
  );
};

export default NotFoundPage;
